# sane_cups_bash_editor
Bash tool to configure SANE and CUPS via CLI.

## Quick install:
```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/DemonDED/sane_cups_bash_editor/refs/heads/develope/installer.sh)"
